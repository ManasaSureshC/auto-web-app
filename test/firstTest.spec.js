suite('Dealer locator component', function(){
    test("Open delaer locator component and wait for it to load completely", function() {
        browser.url('/')
        browser.percySnapshot('Homepage');
    })
})